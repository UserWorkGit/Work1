syntax = "proto3";
service Greeter {
  // Sends a greeting
  rpc Library (Request) returns (Reply) {}
}

// The request message containing name of the book or author.
message Request {
  string name = 1;
}

// The response message containing an array of names.
message Reply {
  string message = 1;
}